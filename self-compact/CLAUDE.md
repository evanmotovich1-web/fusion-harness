# self-compact folder instructions

Before doing implementation, planning, or verification work from this folder, read `prompt.compound.md` and treat it as the task contract for the self-compaction project.

Do not read files under `<PROJECT_ROOT>/specs/*` except your own plan. Do not read files under `<PROJECT_ROOT>/apps/*` except your own work. If you do, stop immediately and report the failure.
